# FieldLog — Mobile App (GC + Sub)

## Scripts

| Command | Purpose |
|---------|---------|
| `npm start` | Start Metro / Expo dev server |
| `npm run android` / `npm run ios` | Open on a device or simulator |
| `npm run typecheck` | TypeScript check |
| `npm run lint` | Expo lint |
